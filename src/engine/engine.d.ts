/**
 * Monta o engine Meridian no DOM: varre os elementos por id, injeta os
 * ícones `[data-icon]`, inicializa parser/editor/layout/export.
 * Chamar uma única vez, após o primeiro render (useEffect).
 */
export declare function mountEngine(): void;
